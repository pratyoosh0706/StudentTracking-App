{
  "framework": "vite",
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "cd client && npm install",
  "env": {
    "VITE_API_URL": "https://studenttracking-app.onrender.com"
  }
}
